App Name & Description:
-Todos
-this is a app that will use the power of React & Vite. Its currently a work in progress and this will be updated as the weekly lessons continue.

Installation instructions:
-within terminal type: "npx create-vite@latest --template react ."
-then "npm install"

How to run the development server:
-within terminal type "npm run dev"
